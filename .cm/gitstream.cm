# -*- mode: yaml -*-

manifest:
  version: 1.0

automations:
  review_new_terraform_module:
    if: 
      - {{ branch.diff.files_metadata | match(attr='file', term='Test' + testfile) | some }}      
    run:
      - action: add-comment@v1
        args:
          comment: |            
             name: hey there bro
    
testfile: {{ branch.diff.files_metadata | map(attr='new_file')}}
