# -*- mode: yaml -*-
manifest:
  version: 1.0
automations:
  review_parameters: 
    if:  
       - {{ source.diff.files | map(attr='original_file') | match(regex=r/^$/) | some }}
    run:
     - action: add-comment@v1
       args: 
         comment: | 
             This file is new.

